import CoordinateNumber from './CoordinateNumber';

class CoordinateNumberYAxis implements CoordinateNumber {
    distance(coord: CoordinateNumberYAxis) {
        return 0;
    }
}

export default CoordinateNumberYAxis;
